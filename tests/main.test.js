import { it, expect, describe } from "vitest";

// Create a Test Suite:
// Name of the test does not matter
// describe("group", () => {});

// Create a Test Case
// it("should", () => {});

describe("group", () => {
  it("should", () => {
    expect(1).toBeTruthy();
  });
});
